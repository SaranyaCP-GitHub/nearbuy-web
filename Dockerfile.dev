# Development Dockerfile for Vue.js application
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Copy source code
COPY . .

# Expose port 80 (to match the container mapping)
EXPOSE 80

# Start development server on port 80 with host binding
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "80"] 